{
  "name": "dev-standards",
  "private": true,
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/bamiyanapp/dev-standards.git"
  },
  "scripts": {
    "lint": "commitlint --last --verbose",
    "lint:workflows": "node scripts/check-workflows.js",
    "test": "node scripts/validate.js && node --test *.test.js scripts/*.test.js && npm run lint:workflows"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.0",
    "@commitlint/config-conventional": "^21.0.0",
    "actionlint": "^2.0.6"
  }
}
